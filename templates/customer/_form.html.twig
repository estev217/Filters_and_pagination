{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-between">
        <a class="btn btn-outline-dark" href="{{ path('customer_index') }}">back to list</a>
        <button class="btn btn-outline-success">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
